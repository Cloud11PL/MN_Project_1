package MathMethods;

public interface UpdateArrays {
     /**
      * Interface with method to update arrays of height, mass, velocity and acceleration
      * @param h
      * @param m
      * @param v
      * @param acc
      */
     void update(double h, double m, double v, double acc);
}
